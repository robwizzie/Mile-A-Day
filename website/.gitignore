# v0 runtime files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# TypeScript incremental build artifact — generated, must not be committed.
# When tracked, Nixpacks mounts a cache dir over the COPY'd file and the
# Docker build fails with "create mountpoint ... not a directory".
*.tsbuildinfo